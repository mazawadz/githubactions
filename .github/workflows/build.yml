name: Build Workflow from the test-branch

on:
  workflow_dispatch:

jobs:
  DummyJob:
    name: Just simple DummyJob
    runs-on: ubuntu-latest
    steps:
      - name: DummyStep
        run: echo Dummy Text!
      - name: Again a one-line script
        run: echo TEST!
